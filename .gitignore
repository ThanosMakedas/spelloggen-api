# Build output
bin/
obj/

# SQLite database, created on first run
*.db
*.db-shm
*.db-wal

# User specific files
*.user
*.suo
.vs/

# Uploaded cover images are runtime data.
# The seeded SVG covers are part of the repo, so keep those.
wwwroot/uploads/*
!wwwroot/uploads/*.svg
